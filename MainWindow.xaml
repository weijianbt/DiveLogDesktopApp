<Window x:Class="DiveLogApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiveLogApplication"
        xmlns:vm="clr-namespace:DiveLogApplication.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000" Loaded="Window_Loaded">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Width" Value="70"/>
                <Setter Property="Height" Value="70"/>
                <Setter Property="Margin" Value="3"/>
            </Style>
        </Grid.Resources>

        <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="1">
            <StackPanel Orientation="Vertical" Margin="5">
                <Button Content="Main Page" Command="{Binding NavigateMainPageCommand}"/>
                <Button Content="User Profile" Command="{Binding NavigateUserProfileCommand}"/>
                <Button Content="Dive logs" Command="{Binding NavigateDiveLogCommand}"/>
                <Button Content="Settings" Command="{Binding NavigateSettingsCommand}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1">
            <Frame NavigationUIVisibility="Hidden" Content="{Binding SelectedContent}"/>
        </Border>

    </Grid>
</Window>
